@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<AdminViewModel>
@{
    ViewData["Title"] = "Index";
}
<br />

   

    <div class="col-md-5">
        <div class="col text-end">
          

        </div>
        <form asp-controller="Admin" asp-action="index" method="get">
            <div class="input-group md-form form-control-sm form-2 p1-0">
                <input type="text" class="form-control my-0 py-1 red-border" placeholder="search" value="" aria-label="Search" name="Adminname" />
                <div class="input-group-append">
                    <input type="submit" id="btnSubmit1" value="Search" class="btn btn-primary"/>


                </div>


            </div>
        </form>




        <br />
       @* <table class="table table-bordered border-primary">
            <thead>
                <tr>
                    <th>User Name</th>
                    <th>Email</th>
                    <th>Action</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.UserName</td>
                       

                        <td>@item.Email</td>
                        <td>
                            <a asp-controller="Admin" asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning">Details</a>
                            <a asp-controller="Admin" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit  <a asp-controller="Admin" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a></a>
                          
                        </td>

                    </tr>
                }
            </tbody>
        </table>*@
    <table class="table table-bordered">
  <thead>
            <tr style="background-color:#37517e;">
      <th scope="col">User name</th>
      <th scope="col">Email</th>
      <th scope="col">Action</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  @foreach (var item in Model)
   {
    <tr>
      <td>@item.UserName</td>
      <td>@item.Email</td>
      <td><a asp-controller="Admin" asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning">Details</a></td>
      <td><a asp-controller="Admin" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a></td>
      <td><a asp-controller="Admin" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a></td>

    </tr>
    
   }
  </tbody>
</table>
   
</div>